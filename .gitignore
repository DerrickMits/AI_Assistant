# dependencies
/node_modules
/.pnp
.pnp.*

# next.js
/.next/
/out/
/build
/tsconfig.tsbuildinfo
next-env.d.ts

# misc
.DS_Store
*.pem
.vscode/
.idea/

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env
.env
.env.local
.env*.local

# vercel
.vercel

# Build-time sibling clone created by `npm run prebuild` (ingest-at-build.ts).
# Never ship or commit it; cleaned up at the end of the build step.
/.kb-sources/

# typescript
*.tsbuildinfo

# Note: /data/knowledgeBase.json is intentionally tracked because it is the
# assistant's committed context snapshot.
